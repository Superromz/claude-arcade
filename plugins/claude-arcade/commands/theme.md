---
description: Switch the Claude Arcade theme - rpg, space or retro (ASCII-only)
argument-hint: "rpg|space|retro"
allowed-tools: Bash(node:*)
---
!`node "${CLAUDE_PLUGIN_ROOT}/scripts/arcade.js" theme $ARGUMENTS`

The command above already ran; don't run anything. Confirm it in one line.
