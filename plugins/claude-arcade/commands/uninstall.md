---
description: Remove the Claude Arcade HUD and restore your previous status line and spinner settings
allowed-tools: Bash(node:*)
---
Run `node "${CLAUDE_PLUGIN_ROOT}/scripts/arcade.js" uninstall` and show the user its output. Mention that to remove the hooks entirely they can run `/plugin uninstall claude-arcade`.