---
description: Remove the Claude Arcade HUD and restore your previous status line and spinner settings
allowed-tools: Bash(node:*)
---
!`node "${CLAUDE_PLUGIN_ROOT}/scripts/arcade.js" uninstall`

The command above already ran; don't run anything. Confirm it in one line, and mention /plugin uninstall claude-arcade removes the hooks too.
