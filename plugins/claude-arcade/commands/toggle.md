---
description: Toggle Claude Arcade options - toasts (level-up and quest messages) or ascii (no emoji)
argument-hint: "toasts|ascii"
allowed-tools: Bash(node:*)
---
Run `node "${CLAUDE_PLUGIN_ROOT}/scripts/arcade.js" toggle $ARGUMENTS` and show the user its output in one line.