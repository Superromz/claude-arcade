---
description: Open the interactive Claude Arcade game in a split pane next to Claude
allowed-tools: Bash(node:*)
---
!`node "${CLAUDE_PLUGIN_ROOT}/scripts/arcade.js" play`

The command above already ran; don't run anything. Relay its instructions to the user in one or two lines, keeping the command in a code block if it printed one.
