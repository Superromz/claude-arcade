{
  "name": "claude-arcade",
  "version": "0.7.0",
  "private": true,
  "description": "A Claude Code plugin that turns the terminal UI into a game.",
  "scripts": {
    "test": "node plugins/claude-arcade/web/client/sync.js --check && node --test",
    "demo": "node demo.js"
  },
  "engines": { "node": ">=18" },
  "license": "MIT"
}